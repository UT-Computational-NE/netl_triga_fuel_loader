# example

A worked example: a core loading pattern and the `specs.py` it generates
(regenerable via the engine's generator). Added in
[#4](https://github.com/UT-Computational-NE/netl_triga_fuel_loader/issues/4).
